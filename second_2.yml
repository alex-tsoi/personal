---
  - hosts: localhost
    name: Volume Action
    vars:
      hostname: mtl-dc-na
      username: admin
      password: 
    tasks:
    - include: createcifs.yml {{ item }}
      with_items:
        - { vol_name: cifs_revdevdmz_webupl_download, vserver: vs_dmzdev_mtl, aggr: aggr02_sata, junction_root: revdevdmz_cifs_root, cifs_share: webupl_download}
        - { vol_name: cifs_revdevdmz_webupl_upload, vserver: vs_dmzdev_mtl, aggr: aggr02_sata, junction_root: revdevdmz_cifs_root, cifs_share: webupl_upload}
